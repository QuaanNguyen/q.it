# llama-server flags for Nemotron 3 Nano 4B on Apple Silicon Metal

Resolves [QuaanNguyen/q.it#10](https://github.com/QuaanNguyen/q.it/issues/10).

## Question

Which `llama-server` flags does `nvidia/NVIDIA-Nemotron3-Nano-4B-Q4_K_M.gguf` (architecture `nemotron_h`, GGUF header `context_length` 1,048,576, hybrid Mamba-2/attention) need on Apple Silicon Metal for correct chat output and sane memory, beyond the launcher's current `--host 127.0.0.1 --port N -m <file> -c <n_ctx> -ngl 999 --parallel 1`?

Sources are llama.cpp `master` (fetched 2026-09-02), the installed Homebrew binary (`llama-server --version`: `0.3.0 (build 10621, commit c1d0e7a00)`, Apple clang, Darwin arm64; `--help` output cited below as "installed `--help`"), and the NVIDIA model cards on Hugging Face. No model was loaded.

## Artifact facts

- HF API GGUF metadata for the repo: `architecture: nemotron_h`, `context_length: 1048576`, `bos_token: <s>`, `eos_token: <|im_end|>`, file size 2,837,072,864 bytes, 3.97B params. Source: [`https://huggingface.co/api/models/nvidia/NVIDIA-Nemotron-3-Nano-4B-GGUF?expand[]=gguf`](https://huggingface.co/api/models/nvidia/NVIDIA-Nemotron-3-Nano-4B-GGUF?expand[]=gguf).
- The GGUF model card states "Context length up to 262K" and "four Attention layers" in a "hybrid architecture consisting primarily of Mamba-2 and MLP layers". Its own llama.cpp example is `./llama-server -hf nvidia/NVIDIA-Nemotron-3-Nano-4B-GGUF:Q4_K_M -c 0 --alias my_model -ngl 999 --port 5000 --host 0.0.0.0`. Source: [model card](https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Nano-4B-GGUF).
- The BF16 parent's `config.json`: `num_hidden_layers: 42`, `hybrid_override_pattern: "M-M-M-MM-M-M*-M-M*-M-M-M*-M-M-MM*-MMM-M-M-"` (21 Mamba `M`, 17 MLP `-`, 4 attention `*`), `num_key_value_heads: 8`, `head_dim: 128`, `mamba_num_heads: 96`, `mamba_head_dim: 80`, `ssm_state_size: 128`, `n_groups: 8`, `conv_kernel: 4`, `max_position_embeddings: 262144`. Source: [config.json](https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Nano-4B-BF16/resolve/main/config.json).
- The GGUF header's 1,048,576 is therefore 4x the HF config's 262,144 and 4x the card's stated 262K. `-c 0` follows the header, not the card.

## Findings

### 1. `--jinja` and chat-template handling

- `--jinja` is on by default in `llama-server`. `common_params::use_jinja = true` in [`common/common.h`](https://github.com/ggml-org/llama.cpp/blob/master/common/common.h) (`bool use_jinja = true;`); `common_params_parser_init` in [`common/arg.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/common/arg.cpp) only flips it to `false` for `LLAMA_EXAMPLE_COMPLETION` and `LLAMA_EXAMPLE_MTMD`, not for `LLAMA_EXAMPLE_SERVER`. The default flipped in [PR #17524 "server: enable jinja by default"](https://github.com/ggml-org/llama.cpp/pull/17524) (merged 2025-11-27). Installed `--help` confirms: `--jinja, --no-jinja  whether to use jinja template engine for chat (default: enabled)`. The [server README](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/README.md) still says tool use "requires `--jinja`", which is now satisfied by the default.
- With jinja, `/v1/chat/completions` applies the GGUF's embedded template. `common_chat_templates_apply` dispatches on `inputs.use_jinja` to `common_chat_templates_apply_jinja` (embedded template) or `common_chat_templates_apply_legacy` in [`common/chat.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/common/chat.cpp).
- Without jinja, the embedded template is not rendered. The legacy path calls `llama_chat_apply_template`, whose detector in [`src/llama-chat.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/src/llama-chat.cpp) maps any template containing `<|im_start|>` (and not `<|im_sep|>`/`<end_of_utterance>`) to the built-in `chatml` renderer. That renderer emits plain `<|im_start|>role\n...<|im_end|>\n` and `<|im_start|>assistant\n`, ignores `enable_thinking`, and the legacy result carries no reasoning parser, so `<think>...</think>` text lands in `content`.
- Nemotron's template needs jinja. The embedded template (from the HF API above) defines `enable_thinking` (default `True`), rewrites assistant history to `<think></think>`-prefixed content, and on `add_generation_prompt` emits `<|im_start|>assistant\n<think>\n` when thinking is on or `<|im_start|>assistant\n<think></think>` when off. It also renders Qwen3-Coder-style XML tools (`<tool_call>`, `<function=`, `<parameter=`). llama.cpp's specialised handler for this template family is selected by exactly those markers: "Qwen3-Coder XML tool calls, also used by Nemotron Nano 3, Qwen3.5 and StepFun-3.5-Flash" in [`common/chat.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/common/chat.cpp), and `common_chat_params_init_qwen3_coder` sets `thinking_start_tag = "<think>"` and `thinking_end_tags = {"</think>", "<tool_call>"}` when the template source contains `<think>`.

### 2. `-fa` flash attention on Metal

- Default is `auto`. `llama_context_default_params` sets `flash_attn_type = LLAMA_FLASH_ATTN_TYPE_AUTO` in [`src/llama-context.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/src/llama-context.cpp); installed `--help`: `-fa, --flash-attn [on|off|auto]  (default: 'auto')`.
- `auto` resolves at graph-reserve time by probing the backend: `if (cparams.auto_fa) { resolve(llm_fused_op_flash_attn_probe, cparams.flash_attn); }` logs "flash_attn enabled" or "not supported, set to disabled" ([`src/llama-context.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/src/llama-context.cpp)). `auto` is also forced to `on` when the V cache is quantized or split mode is tensor.
- Metal supports `GGML_OP_FLASH_ATTN_EXT` for head sizes 32, 40, 48, 64, 72, 80, 96, 112, 128, 192, 256, 320, 512 ([`ggml/src/ggml-metal/ggml-metal-device.m`](https://github.com/ggml-org/llama.cpp/blob/master/ggml/src/ggml-metal/ggml-metal-device.m)). Nemotron 3 Nano 4B's `head_dim` is 128, so `auto` resolves to enabled on Metal without an explicit `-fa on`.

### 3. `-c 0` vs explicit `n_ctx`; what a hybrid model allocates

- `-c 0` means "loaded from model": `cparams.n_ctx = params.n_ctx == 0 ? hparams.n_ctx_train : params.n_ctx` ([`src/llama-context.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/src/llama-context.cpp)); installed `--help`: `-c, --ctx-size N (default: 0, 0 = loaded from model)`. For this GGUF that is 1,048,576.
- `--fit` (default `on`, installed `--help`: `-fit, --fit [on|off] (default: 'on')`) reduces context only when `n_ctx` is unset. An explicit `-c 0` disables that reduction: the `-c` handler sets `fit_params_min_ctx = UINT32_MAX` "if the user explicitly requests the full context size" ([`common/arg.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/common/arg.cpp); [PR #19070](https://github.com/ggml-org/llama.cpp/pull/19070)). An explicit non-zero `-c` is also never changed ("context size set by user ... -> no change" in [`common/fit.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/common/fit.cpp)), and fit aborts with "n_gpu_layers already set by user" when `-ngl` is given. So with the launcher's `-ngl 999`, `--fit` will not rescue any `-c` choice.
- Hybrid memory layout: for `LLM_ARCH_NEMOTRON_H`, `llama_model::create_memory` builds a `llama_memory_hybrid` with `filter_attn = !is_recr(il) && n_ff(il) == 0` and `filter_recr = is_recr(il) && n_ff(il) == 0`, so only the 4 attention layers get a KV cache and only the 21 Mamba layers get recurrent state; the 17 MLP layers get neither ([`src/llama-model.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/src/llama-model.cpp)). The KV cache is sized `attn_kv_size = cparams.n_ctx_seq`; the recurrent store is sized `recurrent_kv_size = max(1, n_seq_max)` in F32 for both R and S ([`src/llama-model.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/src/llama-model.cpp), [`src/llama-memory-hybrid.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/src/llama-memory-hybrid.cpp)). The KV tensors are allocated up front at `kv_size` cells (`ggml_new_tensor_3d(ctx, type_k, n_embd_k_gqa, kv_size, n_stream)` in [`src/llama-kv-cache.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/src/llama-kv-cache.cpp)).
- Recurrent state per layer per sequence: `n_embd_r = (ssm_d_conv - 1) * (ssm_d_inner + 2 * ssm_n_group * ssm_d_state)` and `n_embd_s = ssm_d_state * ssm_d_inner` ([`src/llama-hparams.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/src/llama-hparams.cpp)). With `d_inner = 96 * 80 = 7680`, `n_group = 8`, `d_state = 128`, `d_conv = 4`: `n_embd_r = 29,184`, `n_embd_s = 983,040`, so `(29,184 + 983,040) * 4 B = 3.86 MiB` per Mamba layer, about 81 MiB total for 21 layers at `--parallel 1`. This does not scale with `n_ctx`.
- Attention KV per token: 4 layers x 2 (K,V) x 8 KV heads x 128 x 2 B (F16) = 16 KiB. Derived sizes: 4k -> 64 MiB, 32k -> 512 MiB, 128k -> 2 GiB, 262,144 -> 4 GiB, 1,048,576 (`-c 0`) -> 16 GiB. `-c 0` on this GGUF commits 16 GiB of KV on top of the 2.8 GB weights, which exceeds the stable budget on 16 GB and 24 GB Apple Silicon and is 4x the card's own 262K limit.
- The server warns but does not stop if the slot context exceeds training context ("the slot context (%d) exceeds the training context of the model (%d) - capping" in [`tools/server/server-context.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/server-context.cpp)); explicit `-c` values up to 1,048,576 are accepted silently.

### 4. Reasoning controls and streamed `reasoning_content`

- Control mechanism is the template's `enable_thinking` variable, not `/think` or `/no_think`. The 4B template (HF API above) contains no `/think` or `/no_think` handling; the BF16 card says "If you'd like to use reasoning off, add `enable_thinking=False` to `apply_chat_template()`. By default, `enable_thinking` is set to be True" ([BF16 model card](https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Nano-4B-BF16)). The GGUF card's phrase "controlled via a system prompt" is not backed by anything in the template.
- Server-side flag: `-rea, --reasoning [on|off|auto]` sets `params.enable_reasoning` and `default_template_kwargs["enable_thinking"]` to `"true"`/`"false"`; default `auto` detects from template ([`common/arg.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/common/arg.cpp)). Setting `enable_thinking` via `--chat-template-kwargs` is logged as deprecated in favour of `--reasoning on/off` (same file). Installed `--help` lists `--reasoning`, `--reasoning-effort`, `--reasoning-budget N` ("-1 for unrestricted, 0 for immediate end, N>0 for token budget (default: -1)"), `--reasoning-budget-message`, `--reasoning-preserve`.
- Per-request overrides: `chat_template_kwargs: {"enable_thinking": false}` or OpenAI `reasoning_effort: "none"` both set `inputs.enable_thinking = false` ([`tools/server/server-common.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/server-common.cpp); [server README](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/README.md): "`reasoning_effort`: If `none`, reasoning/thinking is disabled").
- Extraction: `--reasoning-format` default `auto` resolves to `deepseek` (`common_params::reasoning_format = COMMON_REASONING_FORMAT_DEEPSEEK` in [`common/common.h`](https://github.com/ggml-org/llama.cpp/blob/master/common/common.h)); README/`--help`: "deepseek: puts thoughts in `message.reasoning_content`", "none: leaves thoughts unparsed in `message.content`". With this template the PEG parser wraps `<think>...</think>` as reasoning when `extract_reasoning && enable_thinking`, and when thinking is disabled it still consumes the empty `<think></think>` pair from the generation prompt ([`common/chat.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/common/chat.cpp)).
- Streaming: `server_task_result_cmpl_partial` emits `choices[0].delta = server_chat_msg_diff_to_json_oaicompat(diff)`, and that function sets `delta["reasoning_content"] = diff.reasoning_content_delta` and `delta["content"] = diff.content_delta` as separate keys ([`tools/server/server-chat.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/server-chat.cpp), [`tools/server/server-task.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/server-task.cpp)). The diff itself is `string_diff(msg_prv.reasoning_content, msg_new.reasoning_content)` in [`common/chat.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/common/chat.cpp). So thinking tokens arrive as `delta.reasoning_content` chunks, not `delta.content`, unless `--reasoning-format none` is set.

### 5. `/health` semantics and stability

- Documented contract in the [server README](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/README.md): "HTTP status code 503, Body: `{"error": {"code": 503, "message": "Loading model", "type": "unavailable_error"}}`, the model is still being loaded" and "HTTP status code 200, Body: `{"status": "ok" }`, the model is successfully loaded and the server is ready". `/v1/health` is an alias; both are exempt from API-key checks.
- Implementation: the HTTP server is started before model load ("start the HTTP server before loading the model to be able to serve /health requests" in [`tools/server/server.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/server.cpp)); `middleware_server_state` returns 503 with that exact body for every non-UI path while `!is_ready` ([`tools/server/server-http.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/server-http.cpp)); once ready, `get_health` returns `{"status": "ok"}` ([`tools/server/server-context.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/server-context.cpp)). Before the listener is up, connections are refused rather than answered, so a poller must treat connection errors as "still starting" too.
- Stability: this shape dates from [PR #9056 "server : refactor middleware and /health endpoint"](https://github.com/ggml-org/llama.cpp/pull/9056) (merged 2024-08-16), which removed slot state and `fail_on_no_slot` from `/health` and introduced "accessing any endpoint will result in 503" during load. Commit history for the README shows no later `/health` changes ([`git log -- tools/server/README.md`](https://github.com/ggml-org/llama.cpp/commits/master/tools/server/README.md)), and the same PR made a failed model load exit the process with status 1 rather than serve 503 forever.

## Recommendation for qit-runtime's launcher

Keep `--host 127.0.0.1 --port N -m <file> -ngl 999 --parallel 1` and an explicit `-c <n_ctx>` from the session row. Add:

- `--jinja`: redundant on build 10621 but the runtime allows an env-overridden worker binary (CONTEXT.md, "Worker"); on any build before PR #17524 the template falls back to bare chatml and thinking leaks into `content`. Cheap insurance.
- `--reasoning-format deepseek`: pins the current `auto` behaviour so `reasoning_content` deltas are a contract the SSE seam can rely on, independent of future `auto` heuristics.
- `--reasoning on|off` mapped from a per-session or per-smoke-test setting, defaulting to `off` for smoke tests: it is the sanctioned control (`enable_thinking`), `--chat-template-kwargs` is deprecated for this, and `/think` `/no_think` system prompts do nothing for this template. If reasoning is on, expose `--reasoning-budget N` as a cap so a smoke test cannot run away.
- Do not add `-fa`: `auto` resolves to enabled on Metal for head size 128 and degrades gracefully if a future kernel gap appears; `-fa on` would not.
- Never pass `-c 0` for this artifact: it commits 16 GiB of F16 KV for the four attention layers because the GGUF header says 1,048,576 (4x the card's 262K), and `--fit` does not reduce an explicit `-c 0` nor run at all with `-ngl` set. The capacity planner's estimate for `nemotron_h` should be weights + 16 KiB x `n_ctx` (attention KV) + about 81 MiB x `n_parallel` (Mamba state), not a dense-model KV formula over 42 layers.
- Readiness: poll `GET /health`; treat connection refused and 503 as Starting, `200 {"status":"ok"}` as Loaded, and process exit as Failed. No flag is needed.
